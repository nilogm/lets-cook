import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        paddingVertical: 30,
        marginHorizontal: 20,
    },
    number: {
        fontWeight: "bold",
        color: "orange",
        fontSize: 20,
    },
    text: {
        fontSize: 20,
        fontWeight: 300
    },
    textContainer: {
        width: "100%",
        flexDirection: "row",
    },
});

export default styles