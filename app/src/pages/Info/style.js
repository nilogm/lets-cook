import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    infoText: {
        fontSize: 20,
        fontWeight: 300,
    },
    container: {
        margin: 20
    }
});

export default styles