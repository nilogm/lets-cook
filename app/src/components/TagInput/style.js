import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    mainContainer: {
        alignItems: "center",
        paddingBottom: 10,
    },
    inputBox: {
        width: '80%',
        marginBottom: 10,
    },
    
});

export default styles