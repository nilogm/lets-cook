import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: "100%"
    },
    title: {
        margin: 10,
        fontSize: 24,
        fontWeight: 300
    }
})


export default styles