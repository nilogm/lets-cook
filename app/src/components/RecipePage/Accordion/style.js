import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
    },
    title: {
        fontWeight: "bold",
        fontSize: 24,
        marginLeft: 15,
    },
    item_header:{
        fontWeight: "bold",
        fontSize: 20,
    },
    item_info:{
        fontSize: 20,
    },
    button: {
        padding: 5,
        shadowRadius: 2,
        
    }
});

export default styles