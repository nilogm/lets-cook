import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontWeight: "bold",
        fontSize: 28,
        margin: 10,
        marginLeft: 20,
    },
    list_main: {
        height: 160,
    },
    list_lesser: {
        height: 200,
    }
});

export default styles